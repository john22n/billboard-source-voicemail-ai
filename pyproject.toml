[project]
name = "voicemail-agent"
version = "0.1.0"
description = "Billboard Source AI voicemail agent in python and pipecat-ai"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.14.1",
    "pipecat-ai[openai,runner,silero,webrtc,websocket]>=1.5.0",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
]
